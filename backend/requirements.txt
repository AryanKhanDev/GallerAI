# GallерAI — Stage 1 dependencies
# Mirrors pyproject.toml [project.dependencies]
# Use one or the other — both are kept in sync.
#
# Windows + CPU setup:
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt

# Core AI
open-clip-torch>=2.24.0
torch>=2.2.0
torchvision>=0.17.0

# Storage
lancedb>=0.6.0
pyarrow>=14.0.0

# OCR
paddlepaddle>=2.6.0
paddleocr>=2.7.3

# Image handling
Pillow>=10.1.0           # 10.1+ for .avif support

# Data / schemas
pydantic>=2.0.0
numpy>=1.26.0
pandas>=2.0.0

# API — multipart form parsing, required for /upload
python-multipart>=0.0.9

# Dev / testing
pytest>=7.4.0
pytest-cov
rich                           # Pretty CLI output (optional)